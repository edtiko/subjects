import { ContactLinkDirective } from './contact-link.directive';

describe('ContactLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ContactLinkDirective(null);
    expect(directive).toBeTruthy();
  });
});
